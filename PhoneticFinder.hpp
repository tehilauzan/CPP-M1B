#include <iostream>

using namespace std;
namespace phonetic{
    string find(string text, string word) ;
    string chack_Word(string text, string ans,int index);
    int next_word(string text, int i);
    bool same_Char(char x , char y);

    int next_Word(string text, int i);


}
